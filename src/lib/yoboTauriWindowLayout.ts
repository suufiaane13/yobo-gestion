/**
 * Taille minimale une fois connecté (redimensionnement manuel).
 * ~960×640 : navbar + grilles caisse / tableaux restent utilisables.
 */
export const YOBO_APP_MIN_LOGICAL_SIZE = { width: 960, height: 640 } as const
